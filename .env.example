# Base URL of the deployed site. Used for canonical, sitemap, robots, and
# Open Graph URLs. Override in your hosting provider after the custom domain
# is mapped, e.g. https://zerohop.app
NEXT_PUBLIC_SITE_URL=https://zerohop.app